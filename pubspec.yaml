name: molc
description: A Flutter state management package separating UI state Models from business Logic.
version: 0.2.0
homepage: https://github.com/Icyoung/MoLc

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.7.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
